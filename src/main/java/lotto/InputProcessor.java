package lotto;

public class InputProcessor {
    private int tryCount;

    public InputProcessor() {
        this.tryCount = 0;
    }
//    public InputProcessor(String price) {
//        if (price.length() < )
//    }
}
